
// Sound effects have been disabled to ensure a quiet learning environment.

export const playTone = (frequency: number, duration: number = 0.1, type: OscillatorType = 'sine') => {
    // Silent
};

export const playSuccessSound = () => {
    // Silent
};
